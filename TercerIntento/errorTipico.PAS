program HelloWorld;
    const pi = 3.14;
    VAR
    x : integer;
    y : INTEGER;
    z : INTEGER;
begin $
    x := 2;
    y := 3;
    z := x+y;
    writeln(z);
    z := x-y;
    writeln(z);
    z := x*y;
    writeln(z);

end.
